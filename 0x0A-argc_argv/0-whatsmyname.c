#include <stdio.h>
/**
 * main - prints program name with a new line
 * @argc: Number of arguments
 * @argv: array og pointers to argument
 * Return: Always a 0
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
